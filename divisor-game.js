

var divisorGame = function (N) {
    let alicesTurn = true;
    let chalkboardNumber = N;

    // Find highest factor
    // If number is even, largest factor is 1/2
    // If the number is divisible by 3, 
    if (N)



};